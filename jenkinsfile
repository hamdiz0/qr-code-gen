pipeline{
    agent any
    tools {
        nodejs "nodejs-22.9"
    }
    stages{
        stage("build"){
            steps{
                sh "npm --version &&\
                    node --version "
            }
        }
    }
}