pipeline{
    agent any
    tools {
        nodejs "nodejs-22.9"
    }
    stages{
        stage("build"){
            steps{
                echo "npm --version &&\
                      node --version "
            }
        }
    }
}